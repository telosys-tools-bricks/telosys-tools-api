//
// PHP class for entity "${entity.name}" 
// Created on $today.date ( Date ISO $today.date("yyyy-MM-dd") - Time $today.time )
// Generated by $generator.name ( version $generator.version )
//
## $env.setLanguage('xxx')
## // Language : $env.getLanguage() $env.language

/**
 * Domain class for entity "${entity.databaseTable}"
 * 
 * @author Telosys Tools Generator
 *
 */
class $entity.name
{
#foreach( $attribute in $entity.attributes )
	private ${DOLLAR}$attribute.name ; #if ( $attribute.isKeyElement() ) // Id or Primary Key #end

#end

#if ( $entity.hasPrimaryKey() )
	/**
	 * Constructor to allocate an instance and initialize its ID variables.
#foreach( $attribute in $entity.keyAttributes )
	 * @param ${DOLLAR}$attribute.name 
#end
	 */
	public function __construct( $fn.argumentsList($entity.keyAttributes) ) {
#foreach( $attribute in $entity.keyAttributes )
		${DOLLAR}this->$attribute.name  = ${DOLLAR}$attribute.name ;
#end
	}
#end

	/**
	 * Destructor - invoked just before the instance is deallocated.
	 */
	public function __destruct() {
		// Do something here...
	}

#foreach( $attribute in $entity.attributes )
	//--- Attribute $attribute.name
	/**
	 * Getter for $attribute.name.
	 * @return $attribute.name
	 */	
	public function ${attribute.getter}()
	{
		return ${DOLLAR}this->$attribute.name;
    }
	/**
	 * Setter for $attribute.name.
	 * @param ${DOLLAR}$attribute.name
	 */	
	public function ${attribute.setter}(${DOLLAR}$attribute.name)
	{
		${DOLLAR}this->$attribute.name = ${DOLLAR}$attribute.name ;
	}

#end

}
